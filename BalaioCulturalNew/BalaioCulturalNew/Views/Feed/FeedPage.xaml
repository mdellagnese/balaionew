<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             xmlns:behaviors="clr-namespace:Prism.Behaviors;assembly=Prism.Forms"
             xmlns:converters="clr-namespace:Prism.Converters;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="BalaioCulturalNew.Views.Feed.FeedPage"
             Padding="10, 20">
    <!--<ContentPage.Resources>
        <ResourceDictionary>
            <converters:ItemTappedEventArgsConverter x:Key="itemTappedEventArgsConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>-->
    
    <StackLayout>
        <ListView ItemsSource="{Binding Events}"
                  SeparatorVisibility="None"
                  HasUnevenRows="True"
                  BackgroundColor="Transparent"
                  RowHeight="300"
                  Header="">
            <!--<ListView.Behaviors>
                <behaviors:EventToCommandBehavior EventName="ItemTapped" Command="{Binding ItemTappedCommand}">
                    
                </behaviors:EventToCommandBehavior>
            </ListView.Behaviors>-->
            <ListView.HeaderTemplate>
                <DataTemplate>
                    <Grid RowSpacing="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <Label Text="04" 
                                FontSize="Medium" 
                                TextColor="Black"
                                BackgroundColor="Transparent"
                                />
                        <Label  Grid.Row="1" Text="AGO" 
                                FontSize="Small" 
                                TextColor="Black"
                                BackgroundColor="Transparent"
                                />
                    </Grid>
                    
                </DataTemplate>
            </ListView.HeaderTemplate>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid RowSpacing="20" ColumnSpacing="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="50"></RowDefinition>
                                <RowDefinition Height="100"></RowDefinition>
                                <RowDefinition Height="50"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="50"></ColumnDefinition>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                                <ColumnDefinition Width="100"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            
                            <Image 
                                Source="http://lorempixel.com/output/people-q-c-640-480-9.jpg"
                                HeightRequest="200" 
                                Aspect="AspectFill" 
                                Grid.RowSpan="2" 
                                Grid.ColumnSpan="3"></Image>

                            <Label Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3"
                                   Text="Título" 
                                   TextColor="White"
                                   FontSize="Micro"
                                   Margin="5"/>

                            <Label BackgroundColor="Transparent" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="3"
                                   Text="Descrição" 
                                   TextColor="White"
                                   FontSize="Small"
                                   Margin="5"/>

                            <Label HeightRequest="50" VerticalTextAlignment="Center" BackgroundColor="Beige" Grid.Row="2" Grid.Column="0" Text="22h" TextColor="Black"></Label>
                            <Label HeightRequest="50" VerticalTextAlignment="Center" Grid.Row="2" Grid.Column="1" Text="Sinners" TextColor="Black"></Label>
                            <Label HeightRequest="50" VerticalTextAlignment="Center" Grid.Row="2" Grid.Column="2" Text="---" TextColor="Black" HorizontalTextAlignment="End"></Label>

                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>